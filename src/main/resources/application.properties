# Server
server.port=8080


# Spring application
spring.application.name=inventory-system

# Datasource
spring.datasource.url=jdbc:mysql://localhost:3306/mile3
spring.datasource.username=root
spring.datasource.password=Root@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Mail
# Email Configuration for Gmail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sainitin530@gmail.com
spring.mail.password=gsazpayhlzxoohuo
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# App-specific settings
app.jwt.secret=${JWT_SECRET:InventorySystemSecretKeyForJWTSigningAndValidation2024}
app.jwt.expiration=86400000
app.email.from=sainitin530@gmail.com
app.sku.prefix=SKU-
app.sku.length=6
app.low-stock-threshold=10
app.permanent-delete-days=30

# Logging
logging.level.com.inventory=DEBUG
logging.level.org.springframework.security=DEBUG
 my application.propertie sfile code i